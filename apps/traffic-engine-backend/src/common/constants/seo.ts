/** Minimum SEO score before running optimize step (inclusive boundary: below triggers optimize). */
export const SEO_SCORE_OPTIMIZE_THRESHOLD = 70;

/** Maximum number of optimize→analyze cycles after initial analyze. */
export const MAX_OPTIMIZATION_LOOPS = 2;
